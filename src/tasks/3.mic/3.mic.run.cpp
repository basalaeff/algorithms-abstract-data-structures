#include "3.mic.hpp"

// ============================================================================
// ЗАПУСК
// ============================================================================
// Выполняет полный сценарий: парсинг, поиск максимума и форматирование.
// ============================================================================

std::string Mic::run(const std::string& query) {
  parse(query);

  findMaxIntersection();

  return formatResult();
}
